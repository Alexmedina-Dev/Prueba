{
  "name": "motoverso",
  "version": "1.0.0",
  "description": "MotoVerso - Sistema de gestión de taller (Fullstack)",
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "cd backend && npm install",
    "dev": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
